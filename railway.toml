[build]
builder = "dockerfile"
dockerfilePath = "mymirro-chat-api/Dockerfile"
watchPatterns = ["mymirro-chat-api/**"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/test-env"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

